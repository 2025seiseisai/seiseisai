{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Bun: debug",
            "type": "node-terminal",
            "request": "launch",
            "command": "bun --bun run dev",
            "cwd": "${workspaceFolder}"
        }
    ]
}
