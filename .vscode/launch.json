{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Admin Dev",
            "type": "node-terminal",
            "request": "launch",
            "command": "bun run dev:admin",
            "cwd": "${workspaceFolder}"
        },
        {
            "name": "Run Tickets Dev",
            "type": "node-terminal",
            "request": "launch",
            "command": "bun run dev:tickets",
            "cwd": "${workspaceFolder}"
        }
    ]
}
